import React from "react";
import { Flex } from "@chakra-ui/react";

const Applies = () => {
  return (
    <Flex as="section" h="65vh">
      Applies dashboard
    </Flex>
  );
};

export default Applies;
